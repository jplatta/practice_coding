# Some Notes on Functional Programming

## Synonyms in Ruby
* `reduce` and `inject`
* `size` and `length`
* `collect` and `map`
* `select` and `find_all`
* `detect` and `find`
* `reject` is the opposite of `collect`