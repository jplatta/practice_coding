# Functions & Blocks in Ruby

## Blocks

### Some notes on blocks:
* Blocks are anonymous functions.
* _Every_ Ruby method can take a block as a parameter.
* You can invoke a block with the `yield` keyword.
